name: moelung_new
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_map: ^8.1.1
  fluttertoast: ^8.2.12
  getwidget: ^6.0.0
  shared_preferences: ^2.0.0 # Add shared_preferences
  geolocator: ^12.0.0 # Add geolocator

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/
